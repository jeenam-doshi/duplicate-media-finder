# Duplicate Media Finder

A high-performance, AI-driven cross-media duplicate detector that uses deep neural network embeddings to analyze the actual visual content of images and videos—immune to renaming, resizing, or re-encoding.

---

## Tech Stack:-
* **Python**, **PyTorch** & **torchvision** (ResNet-50 deep feature extraction)
* **OpenCV** (Video keyframe sampling & stream analysis)
* **Scikit-learn** (Pairwise cosine similarity matrix computation)
* **Pillow & NumPy** (Image processing & matrix manipulation)

---

## Project Structure:-
```text
duplicate-media-finder/
│
├── finder.py              # Core AI engine (ResNet-50 feature extraction & folder profiling)
├── main.py                # Interactive CLI with smart recommendations & visual inspector
├── requirements.txt       # Project dependencies
├── .gitignore             # Ignore rules for caches, venv, and local media
│
├── assets/                # Demo screenshots and visual assets
│   ├── demo-1.png
│   └── demo-2.png
│
└── samples/               # Sample test files for scanning
    ├── sample-image-1.jpg
    ├── sample-image-2.webp
    ├── sample-video-1.mp4
    └── sample-video-2.mp4

## Usage & Threshold Guide:-
*Select option 1 and drag and drop your media folder into the terminal (quotes/spaces are cleaned automatically).
*Review the Folder Profile Report generated by the AI:
1.Mixed Media / Videos: Recommended threshold ~ 0.80 – 0.85 (catches cross-format or varied angles).
2.Static Images: Recommended threshold ~ 0.90 – 0.95 (catches exact duplicates or compressed copies).
*Press Enter to accept the AI recommendation or input a custom threshold value.
*Choose whether to open the detected duplicate groups instantly for visual inspection and clean-up.